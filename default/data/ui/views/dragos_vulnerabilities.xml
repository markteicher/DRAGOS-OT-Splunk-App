<!--
=============================================================================
 default/data/ui/views/dragos_vulnerabilities.xml
 Dragos OT Security for Splunk App
 Vulnerabilities — Hardware & Firmware
=============================================================================

PURPOSE

Displays all vulnerability detections retrieved from the Dragos Platform
using the Dragos REST API endpoint:

  GET /api/v1/vulnerabilities?expand=asset,report

DATA SOURCE

/api/v1/vulnerabilities
→ sourcetype=dragos:vulnerabilities

FIELDS DISPLAYED

Top-level vulnerability fields:
- id
- severity
- status
- firstSeenAt
- lastSeenAt
- count
- cve
- cvssScore
- description
- remediation

Asset context:
- asset.id
- asset.name
- asset.ipAddress
- asset.macAddress
- asset.vendor
- asset.model
- asset.site
- asset.zone

Report context:
- report.id
- report.title
- report.publishedAt
- report.reference

=============================================================================
-->

<dashboard version="1.1" theme="light">
  <label>Vulnerabilities — Hardware & Firmware</label>

  <row>
    <panel>
      <table>
        <search>
          <query>
            index=* sourcetype=dragos:vulnerabilities
            | spath
            | table
                id
                severity
                status
                firstSeenAt
                lastSeenAt
                count
                cve
                cvssScore
                description
                remediation
                asset.id
                asset.name
                asset.ipAddress
                asset.macAddress
                asset.vendor
                asset.model
                asset.site
                asset.zone
                report.id
                report.title
                report.publishedAt
                report.reference
            | sort - lastSeenAt
          </query>
        </search>
        <option name="wrap">true</option>
        <option name="rowNumbers">true</option>
      </table>
    </panel>
  </row>

</dashboard>
