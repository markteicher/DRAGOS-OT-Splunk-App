<dashboard version="1.1">
  <label>Dragos OT Security – Overview</label>

  <!-- ========================= -->
  <!-- HEADER WITH DRAGOS LOGO -->
  <!-- ========================= -->
  <row>
    <panel>
      <html>
        <![CDATA[
        <div style="display:flex; align-items:center; padding:10px 0;">
          <img src="/static/app/dragos_splunk_app/appLogo.png"
               alt="Dragos"
               style="height:48px; margin-right:16px;" />
          <h1 style="margin:0; font-weight:600;">
            Dragos OT Security – Overview
          </h1>
        </div>
        ]]>
      </html>
    </panel>
  </row>

  <!-- ========================= -->
  <!-- HIGH-LEVEL KPIs -->
  <!-- ========================= -->
  <row>
    <panel>
      <title>Total Alerts</title>
      <single>
        <search>
          <query>
            index=dragos sourcetype=dragos:alerts
            | stats count
          </query>
          <earliest>-24h</earliest>
          <latest>now</latest>
        </search>
        <option name="unit"></option>
      </single>
    </panel>

    <panel>
      <title>High Severity Alerts</title>
      <single>
        <search>
          <query>
            index=dragos sourcetype=dragos:alerts severity=high
            | stats count
          </query>
          <earliest>-24h</earliest>
          <latest>now</latest>
        </search>
      </single>
    </panel>

    <panel>
      <title>Unique Assets Observed</title>
      <single>
        <search>
          <query>
            index=dragos sourcetype=dragos:assets
            | stats dc(asset_id)
          </query>
          <earliest>-24h</earliest>
          <latest>now</latest>
        </search>
      </single>
    </panel>
  </row>

  <!-- ========================= -->
  <!-- ALERT SEVERITY BREAKDOWN -->
  <!-- ========================= -->
  <row>
    <panel>
      <title>Alerts by Severity</title>
      <chart>
        <search>
          <query>
            index=dragos sourcetype=dragos:alerts
            | stats count by severity
          </query>
          <earliest>-24h</earliest>
          <latest>now</latest>
        </search>
        <option name="charting.chart">column</option>
        <option name="charting.legend.placement">right</option>
      </chart>
    </panel>
  </row>

  <!-- ========================= -->
  <!-- RECENT ALERTS TABLE -->
  <!-- ========================= -->
  <row>
    <panel>
      <title>Recent Alerts</title>
      <table>
        <search>
          <query>
            index=dragos sourcetype=dragos:alerts
            | sort -timestamp
            | table timestamp alert_name severity confidence threat_family protocol asset_name
          </query>
          <earliest>-24h</earliest>
          <latest>now</latest>
        </search>
        <option name="count">10</option>
      </table>
    </panel>
  </row>

</dashboard>
